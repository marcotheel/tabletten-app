const K_MED="meds_dash_v1",K_HIST="hist_dash_v1",K_SERVER="pzn_server_url",K_THEME="theme";let meds=JSON.parse(localStorage.getItem(K_MED)||"[]"),hist=JSON.parse(localStorage.getItem(K_HIST)||"[]"),editId=null,stream=null,detector=null,scanTimer=null;const $=id=>document.getElementById(id),ids=["name","substance","strength","manufacturer","pzn","packSize","stock","dosePerTake","limit","times","note"],today=()=>new Date().toISOString().slice(0,10),save=()=>{localStorage.setItem(K_MED,JSON.stringify(meds));localStorage.setItem(K_HIST,JSON.stringify(hist))},esc=s=>String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m])),escJs=s=>String(s??"").replace(/\\/g,"\\\\").replace(/'/g,"\\'"),times=s=>String(s||"").split(",").map(x=>x.trim()).filter(Boolean),fmt=d=>d.toLocaleDateString("de-DE",{day:"2-digit",month:"2-digit",year:"numeric"}),now=()=>new Date().toLocaleString("de-DE",{day:"2-digit",month:"2-digit",year:"numeric",hour:"2-digit",minute:"2-digit"});if(localStorage.getItem(K_THEME)==="dark")document.body.classList.add("dark");$("serverUrl").value=localStorage.getItem(K_SERVER)||"http://localhost:5000";function page(name){document.querySelectorAll(".page").forEach(p=>p.classList.remove("active"));$("page-"+name).classList.add("active");document.querySelectorAll(".bottom-nav button").forEach(b=>b.classList.toggle("active",b.dataset.page===name));window.scrollTo({top:0,behavior:"smooth"})}document.querySelectorAll("[data-page]").forEach(b=>b.onclick=()=>page(b.dataset.page));$("quickAddBtn").onclick=()=>newForm();$("newMedBtn").onclick=()=>newForm();function normalizePzn(v){let s=String(v||"").replace(/\D/g,"");if(s.length>8)s=s.slice(-8);return s.replace(/^0+/,"")||s}function getServerUrl(){return($("serverUrl").value||"").replace(/\/$/,"")}async function lookupPzn(){const pzn=normalizePzn($("pzn").value),server=getServerUrl();if(!server){alert("Bitte Server-URL unter Mehr eintragen.");return}if(!pzn){alert("Bitte zuerst PZN scannen oder eingeben.");return}$("pzn").value=pzn;$("lookupInfo").textContent="PZN wird gesucht...";try{const res=await fetch(`${server}/api/pzn/${encodeURIComponent(pzn)}`);if(res.status===404){$("lookupInfo").textContent="PZN nicht gefunden. Server-Datenbank erweitern.";return}if(!res.ok)throw Error();const d=await res.json();$("name").value=d.name||"";$("substance").value=d.substance||"";$("strength").value=d.strength||"";$("manufacturer").value=d.manufacturer||"";if(d.packSize)$("packSize").value=d.packSize;$("lookupInfo").textContent="PZN gefunden. Angaben bitte prüfen."}catch(e){$("lookupInfo").textContent="PZN-Server nicht erreichbar. Server-URL prüfen."}}function calc(m){const takes=(m.times?.length||1),dose=Number(m.dosePerTake||1)*takes,days=Math.floor(Number(m.stock||0)/Math.max(dose,.1)),usable=Number(m.stock||0)-Number(m.limit||0),remind=usable<=0?0:Math.floor(usable/Math.max(dose,.1));const empty=new Date();empty.setDate(empty.getDate()+days);const reminder=new Date();reminder.setDate(reminder.getDate()+remind);return{days,remind,empty,reminder,daily:dose}}function status(m){const c=calc(m);if(Number(m.stock)<=Number(m.limit))return["crit","Sofort bestellen"];if(c.remind<=7)return["warn","Bald bestellen"];return["ok","Ausreichend"]}function taken(m,t){return hist.some(h=>h.type==="taken"&&h.medId===m.id&&h.date===today()&&h.time===t)}function dueItems(){let a=[];meds.forEach(m=>(m.times?.length?m.times:["manuell"]).forEach(t=>{if(!taken(m,t))a.push([m,t])}));return a}function render(){$("total").textContent=meds.length;$("due").textContent=dueItems().length;$("critical").textContent=meds.filter(m=>status(m)[0]==="crit").length;$("todayTitle").textContent=dueItems().length?`${dueItems().length} Einnahme(n) offen`:"Alles erledigt";renderToday();renderOrders();renderHistory();renderMeds()}function renderToday(){const d=dueItems();$("todayList").innerHTML=d.length?"":'<div class="muted">Heute ist alles erledigt.</div>';d.slice(0,6).forEach(([m,t])=>$("todayList").insertAdjacentHTML("beforeend",`<div class="item"><b>${esc(m.name)}</b> <span class="badge">${esc(t)}</span><br><span class="muted">${esc(m.strength||"")} ${esc(m.substance||"")}</span><button class="green" onclick="take(${m.id},'${escJs(t)}')">Eingenommen</button></div>`))}function renderOrders(){const o=meds.filter(m=>status(m)[0]!=="ok");$("orderPreview").innerHTML=o.length?"":"Aktuell muss nichts nachbestellt werden.";$("orderList").innerHTML=o.length?"":"Aktuell muss nichts nachbestellt werden.";o.forEach(m=>$("orderList").insertAdjacentHTML("beforeend",`• ${esc(m.name)} ${esc(m.strength||"")} | PZN: ${esc(m.pzn||"-")} | Bestand: ${esc(m.stock)}<br>`));o.slice(0,4).forEach(m=>$("orderPreview").insertAdjacentHTML("beforeend",`<div class="item"><b>${esc(m.name)}</b><br><span class="muted">Rest: ${esc(m.stock)} Stück · PZN: ${esc(m.pzn||"-")}</span></div>`))}function renderHistory(){$("historyList").innerHTML=hist.length?"":'<div class="muted">Noch kein Protokoll vorhanden.</div>';hist.slice().reverse().slice(0,50).forEach(h=>$("historyList").insertAdjacentHTML("beforeend",`<div class="item"><b>${esc(h.medName)}</b><br>${h.type==="taken"?"Eingenommen":"Neue Packung"} · ${esc(h.time)}<br><span class="muted">${esc(h.created)}</span></div>`))}function renderMeds(){$("medList").innerHTML=meds.length?"":'<div class="item muted">Noch kein Medikament angelegt.</div>';meds.slice().sort((a,b)=>calc(a).remind-calc(b).remind).forEach(m=>{const c=calc(m),s=status(m);$("medList").insertAdjacentHTML("beforeend",`<div class="med ${s[0]}"><h3>💊 ${esc(m.name)} <span class="badge ${s[0]}">${s[1]}</span></h3>${m.substance?`Wirkstoff: <b>${esc(m.substance)}</b><br>`:""}${m.strength?`Stärke/Form: <b>${esc(m.strength)}</b><br>`:""}${m.manufacturer?`Hersteller: <b>${esc(m.manufacturer)}</b><br>`:""}${m.pzn?`PZN/Barcode: <b>${esc(m.pzn)}</b><br>`:""}Einnahmezeiten: <b>${esc((m.times||[]).join(", ")||"manuell")}</b><br>Bestand: <b>${esc(m.stock)} Stück</b><br>Reicht noch: <b>${c.days} Tage</b><br>Nachbestellen ab: <b>${fmt(c.reminder)}</b>${m.note?`<p class="muted">${esc(m.note)}</p>`:""}<div class="grid"><button class="green" onclick="take(${m.id},'manuell')">Jetzt eingenommen</button><button class="orange" onclick="pack(${m.id})">Neue Packung</button><button onclick="edit(${m.id})">Bearbeiten</button><button class="danger" onclick="delMed(${m.id})">Löschen</button></div></div>`)})}function readForm(){return{id:editId||Date.now(),name:$("name").value.trim(),substance:$("substance").value.trim(),strength:$("strength").value.trim(),manufacturer:$("manufacturer").value.trim(),pzn:normalizePzn($("pzn").value),packSize:+$("packSize").value,stock:+$("stock").value,dosePerTake:+$("dosePerTake").value,limit:+$("limit").value,times:times($("times").value),note:$("note").value.trim()}}function clearForm(){editId=null;ids.forEach(id=>$(id).value="");$("dosePerTake").value=1;$("limit").value=10;$("lookupInfo").textContent="";$("formTitle").textContent="Medikament anlegen";$("saveBtn").textContent="Speichern"}function newForm(){clearForm();page("form")}function saveForm(){const m=readForm();if(!m.name||m.packSize<1||m.stock<0||m.dosePerTake<=0||m.limit<0){alert("Bitte Pflichtfelder korrekt ausfüllen.");return}meds=editId?meds.map(x=>x.id===editId?m:x):[...meds,m];save();clearForm();render();page("meds")}function edit(id){const m=meds.find(x=>x.id===id);if(!m)return;editId=id;ids.forEach(k=>$(k).value=k==="times"?(m.times||[]).join(", "):(m[k]??""));$("formTitle").textContent="Medikament bearbeiten";$("saveBtn").textContent="Änderung speichern";page("form")}function delMed(id){if(confirm("Medikament löschen?")){meds=meds.filter(m=>m.id!==id);save();render()}}function take(id,t){const m=meds.find(x=>x.id===id);if(!m)return;m.stock=Math.max(0,Number(m.stock)-Number(m.dosePerTake||1));hist.push({id:Date.now(),medId:id,medName:m.name,type:"taken",time:t,date:today(),created:now()});save();render()}function pack(id){const m=meds.find(x=>x.id===id);if(!m)return;m.stock=Number(m.stock)+Number(m.packSize||0);hist.push({id:Date.now(),medId:id,medName:m.name,type:"pack",time:"Packung",date:today(),created:now()});save();render()}async function scan(){if(!("BarcodeDetector" in window)){alert("Barcode-Scanner wird nicht unterstützt. Bitte PZN manuell eingeben.");return}try{detector=new BarcodeDetector({formats:["ean_13","ean_8","code_128","code_39","qr_code"]});$("scanner").classList.remove("hidden");stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:"environment"}});$("video").srcObject=stream;scanTimer=setInterval(async()=>{try{const codes=await detector.detect($("video"));if(codes.length){$("pzn").value=normalizePzn(codes[0].rawValue);$("scanInfo").textContent="PZN/Barcode erkannt: "+$("pzn").value;stopScan();lookupPzn()}}catch{}},500)}catch(e){alert("Kamera konnte nicht gestartet werden.");stopScan()}}function stopScan(){if(scanTimer)clearInterval(scanTimer);if(stream)stream.getTracks().forEach(t=>t.stop());$("scanner").classList.add("hidden")}function exportBackup(){const blob=new Blob([JSON.stringify({version:"dashboard-v1",meds,hist},null,2)],{type:"application/json"}),a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download="tabletten-backup-dashboard-v1.json";a.click()}function importBackup(f){const r=new FileReader();r.onload=()=>{try{const d=JSON.parse(r.result);meds=d.meds||d.medicines||[];hist=d.hist||d.history||[];save();render();alert("Backup importiert.")}catch{alert("Backup ungültig.")}};r.readAsText(f)}function copyOrders(){const o=meds.filter(m=>status(m)[0]!=="ok").map(m=>`- ${m.name} ${m.strength||""} | PZN: ${m.pzn||"-"} | Bestand: ${m.stock}`).join("\n")||"Aktuell muss nichts nachbestellt werden.";navigator.clipboard?.writeText(o).then(()=>alert("Kopiert."),()=>alert(o))}$("saveServerBtn").onclick=()=>{localStorage.setItem(K_SERVER,getServerUrl());alert("Server gespeichert.")};$("lookupBtn").onclick=lookupPzn;$("saveBtn").onclick=saveForm;$("cancelBtn").onclick=()=>{clearForm();page("meds")};$("scanBtn").onclick=scan;$("stopScanBtn").onclick=stopScan;$("exportBtn").onclick=exportBackup;$("importBtn").onclick=()=>$("importFile").click();$("importFile").onchange=e=>e.target.files[0]&&importBackup(e.target.files[0]);$("clearHistoryBtn").onclick=()=>{if(confirm("Protokoll löschen?")){hist=[];save();render()}};$("copyOrderBtn").onclick=copyOrders;$("themeBtn").onclick=()=>{document.body.classList.toggle("dark");localStorage.setItem(K_THEME,document.body.classList.contains("dark")?"dark":"light")};if("serviceWorker" in navigator)navigator.serviceWorker.register("service-worker.js");render();