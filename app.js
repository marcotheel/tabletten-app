const K_MED='meds_v5',K_HIST='hist_v5',K_THEME='theme';let meds=JSON.parse(localStorage.getItem(K_MED)||'[]'),hist=JSON.parse(localStorage.getItem(K_HIST)||'[]');let editId=null,stream=null,detector=null,scanTimer=null;const $=id=>document.getElementById(id);const fields=['name','substance','strength','manufacturer','pzn','packSize','stock','dosePerTake','limit','times','note'];const today=()=>new Date().toISOString().slice(0,10);const save=()=>{localStorage.setItem(K_MED,JSON.stringify(meds));localStorage.setItem(K_HIST,JSON.stringify(hist));};const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));const parseTimes=s=>String(s||'').split(',').map(x=>x.trim()).filter(Boolean);const fmt=d=>d.toLocaleDateString('de-DE',{day:'2-digit',month:'2-digit',year:'numeric'});const now=()=>new Date().toLocaleString('de-DE',{day:'2-digit',month:'2-digit',year:'numeric',hour:'2-digit',minute:'2-digit'});if(localStorage.getItem(K_THEME)==='dark')document.body.classList.add('dark');
function calc(m){const takes=(m.times?.length||1),daily=Number(m.dosePerTake||1)*takes,days=Math.floor(Number(m.stock||0)/Math.max(daily,.1)),usable=Number(m.stock||0)-Number(m.limit||0),remind=usable<=0?0:Math.floor(usable/Math.max(daily,.1));let empty=new Date(),reminder=new Date();empty.setDate(empty.getDate()+days);reminder.setDate(reminder.getDate()+remind);return{daily,days,remind,empty,reminder};}
function status(m){const c=calc(m);if(Number(m.stock)<=Number(m.limit))return['crit','Sofort bestellen'];if(c.remind<=7)return['warn','Bald bestellen'];return['ok','Ausreichend'];}
function taken(m,t){return hist.some(h=>h.type==='taken'&&h.medId===m.id&&h.date===today()&&h.time===t)}
function dueItems(){let a=[];meds.forEach(m=>(m.times?.length?m.times:['manuell']).forEach(t=>{if(!taken(m,t))a.push([m,t])}));return a;}
function render(){document.getElementById('total').textContent=meds.length;document.getElementById('due').textContent=dueItems().length;document.getElementById('critical').textContent=meds.filter(m=>status(m)[0]==='crit').length;renderToday();renderHistory();renderOrders();renderMeds();}
function renderToday(){const d=dueItems();todayList.innerHTML=d.length?'':'<div class="muted">Heute ist alles erledigt.</div>';d.forEach(([m,t])=>todayList.insertAdjacentHTML('beforeend',`<div class="item"><b>${esc(m.name)}</b> <span class="badge">${esc(t)}</span><br><span class="muted">${esc(m.strength||'')} ${esc(m.substance||'')}</span><button class="green" onclick="take(${m.id},'${esc(t)}')">Eingenommen</button></div>`));}
function renderHistory(){historyList.innerHTML=hist.length?'':'<div class="muted">Noch kein Protokoll vorhanden.</div>';hist.slice().reverse().slice(0,40).forEach(h=>historyList.insertAdjacentHTML('beforeend',`<div class="item"><b>${esc(h.medName)}</b><br>${h.type==='taken'?'Eingenommen':'Neue Packung'} · ${esc(h.time)}<br><span class="muted">${esc(h.created)}</span></div>`));}
function renderOrders(){const o=meds.filter(m=>status(m)[0]!=='ok');orderList.innerHTML=o.length?'':'Aktuell muss nichts nachbestellt werden.';o.forEach(m=>orderList.insertAdjacentHTML('beforeend',`• ${esc(m.name)} ${esc(m.strength||'')} | PZN: ${esc(m.pzn||'-')} | Bestand: ${esc(m.stock)}<br>`));}
function renderMeds(){medList.innerHTML=meds.length?'':'<div class="item muted">Noch kein Medikament angelegt.</div>';meds.slice().sort((a,b)=>calc(a).remind-calc(b).remind).forEach(m=>{const c=calc(m),s=status(m);medList.insertAdjacentHTML('beforeend',`<div class="med ${s[0]}"><h3>💊 ${esc(m.name)} <span class="badge ${s[0]}">${s[1]}</span></h3>${m.substance?`Wirkstoff: <b>${esc(m.substance)}</b><br>`:''}${m.strength?`Stärke: <b>${esc(m.strength)}</b><br>`:''}${m.manufacturer?`Hersteller: <b>${esc(m.manufacturer)}</b><br>`:''}${m.pzn?`PZN/Barcode: <b>${esc(m.pzn)}</b><br>`:''}Einnahmezeiten: <b>${esc((m.times||[]).join(', ')||'manuell')}</b><br>Dosis je Einnahme: <b>${esc(m.dosePerTake)}</b><br>Bestand: <b>${esc(m.stock)} Stück</b><br>Tagesverbrauch: <b>${c.daily} Stück</b><br>Reicht noch: <b>${c.days} Tage</b><br>Nachbestellen ab: <b>${fmt(c.reminder)}</b><br>Leer am: <b>${fmt(c.empty)}</b><br>${m.note?`<p class="muted">${esc(m.note)}</p>`:''}<div class="grid"><button class="green" onclick="take(${m.id},'manuell')">Jetzt eingenommen</button><button class="orange" onclick="pack(${m.id})">Neue Packung</button><button onclick="edit(${m.id})">Bearbeiten</button><button class="danger" onclick="delMed(${m.id})">Löschen</button></div></div>`);});}
function readForm(){return{id:editId||Date.now(),name:name.value.trim(),substance:substance.value.trim(),strength:strength.value.trim(),manufacturer:manufacturer.value.trim(),pzn:pzn.value.trim(),packSize:+packSize.value,stock:+stock.value,dosePerTake:+dosePerTake.value,limit:+limit.value,times:parseTimes(times.value),note:note.value.trim()};}
function clearForm(){editId=null;fields.forEach(id=>$(id).value='');dosePerTake.value=1;limit.value=10;saveBtn.textContent='Speichern';cancelBtn.classList.add('hidden');}
function saveForm(){const m=readForm();if(!m.name||m.packSize<1||m.stock<0||m.dosePerTake<=0||m.limit<0){alert('Bitte Pflichtfelder korrekt ausfüllen.');return}meds=editId?meds.map(x=>x.id===editId?m:x):[...meds,m];save();clearForm();render();}
function edit(id){const m=meds.find(x=>x.id===id);if(!m)return;editId=id;fields.forEach(k=>$(k).value=k==='times'?(m.times||[]).join(', '):(m[k]??''));saveBtn.textContent='Änderung speichern';cancelBtn.classList.remove('hidden');scrollTo({top:0,behavior:'smooth'});}
function delMed(id){if(confirm('Medikament löschen?')){meds=meds.filter(m=>m.id!==id);save();render();}}
function take(id,t){const m=meds.find(x=>x.id===id);if(!m)return;m.stock=Math.max(0,Number(m.stock)-Number(m.dosePerTake||1));hist.push({id:Date.now(),medId:id,medName:m.name,type:'taken',time:t,date:today(),created:now()});save();render();}
function pack(id){const m=meds.find(x=>x.id===id);if(!m)return;m.stock=Number(m.stock)+Number(m.packSize||0);hist.push({id:Date.now(),medId:id,medName:m.name,type:'pack',time:'Packung',date:today(),created:now()});save();render();}
function exportBackup(){const blob=new Blob([JSON.stringify({version:5,meds,hist},null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='tabletten-backup-v5.json';a.click();}
function importBackup(f){const r=new FileReader();r.onload=()=>{try{const d=JSON.parse(r.result);meds=d.meds||d.medicines||[];hist=d.hist||d.history||[];save();render();alert('Backup importiert.');}catch{alert('Backup ungültig.');}};r.readAsText(f);}
async function scan(){if(!('BarcodeDetector'in window)){alert('Barcode-Scanner wird von diesem Browser nicht unterstützt. Bitte PZN manuell eingeben.');return}try{detector=new BarcodeDetector({formats:['ean_13','ean_8','code_128','code_39','qr_code']});scanner.classList.remove('hidden');stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:'environment'}});video.srcObject=stream;scanTimer=setInterval(async()=>{try{const codes=await detector.detect(video);if(codes.length){pzn.value=codes[0].rawValue;stopScan();}}catch{}},500);}catch(e){alert('Kamera konnte nicht gestartet werden.');stopScan();}}
function stopScan(){if(scanTimer)clearInterval(scanTimer);if(stream)stream.getTracks().forEach(t=>t.stop());scanner.classList.add('hidden');}
function copyOrders(){const o=meds.filter(m=>status(m)[0]!=='ok').map(m=>`- ${m.name} ${m.strength||''} | PZN: ${m.pzn||'-'} | Bestand: ${m.stock}`).join('\n')||'Aktuell muss nichts nachbestellt werden.';navigator.clipboard?.writeText(o).then(()=>alert('Kopiert.'),()=>alert(o));}
saveBtn.onclick=saveForm;cancelBtn.onclick=clearForm;themeBtn.onclick=()=>{document.body.classList.toggle('dark');localStorage.setItem(K_THEME,document.body.classList.contains('dark')?'dark':'light')};scanBtn.onclick=scan;stopScanBtn.onclick=stopScan;exportBtn.onclick=exportBackup;importBtn.onclick=()=>importFile.click();importFile.onchange=e=>e.target.files[0]&&importBackup(e.target.files[0]);clearHistoryBtn.onclick=()=>{if(confirm('Protokoll löschen?')){hist=[];save();render();}};copyOrderBtn.onclick=copyOrders;if('serviceWorker'in navigator)navigator.serviceWorker.register('service-worker.js');render();
